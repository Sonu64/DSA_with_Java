public class clear_range_of_bits {

    public static int clearRangeOfBits(int num, int i, int j) {
        int totalBits = (j-i)+1;
        // We have to clear last totalBits from position i

    }
}
